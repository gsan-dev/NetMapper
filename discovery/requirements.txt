# Descubrimiento de redes y hosts
netifaces2>=0.0.22
pyroute2>=0.7,<0.8 ; sys_platform == "linux"  # solo Linux (usa netlink sockets)
pysnmp>=6.2,<6.3  # fijado a la serie 6.2.x: su hlapi asíncrono (pysnmp.hlapi.asyncio con walkCmd) es
                   # incompatible tanto con 6.1.x (pysnmp.hlapi.v3arch.asyncio, walk_cmd) como con 7.x
                   # (pysnmp.hlapi.asyncio, pero de vuelta a walk_cmd) — un rango amplio rompía el
                   # import en silencio según qué versión resolviera pip en cada instalación.
scapy>=2.5,<3.0

# Caracterización de dispositivos
manuf>=1.1,<2.0
zeroconf>=0.130,<1.0
cryptography>=42,<43  # inspección de certificados TLS (mejora futura ya implementada)

# Integración con NetGuardian (mejora futura ya implementada)
requests>=2.31,<3.0

# Config
python-dotenv>=1.0,<2.0
